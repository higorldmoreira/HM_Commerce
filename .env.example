# ---------------------------------------------------------------
# HM Commerce — environment variables
# Copy to .env and fill in real values before running.
# NEVER commit the filled-in .env to version control.
# ---------------------------------------------------------------

# MySQL ─────────────────────────────────────────────────────────
DB_ROOT_PASSWORD=changeme_root
DB_USER=commerce
DB_PASSWORD=changeme
DB_NAME=rx

# JWT ───────────────────────────────────────────────────────────
# This value maps to "HashCode" in appsettings.json
# Generate with:  openssl rand -base64 64
HASH_CODE=replace_with_a_long_random_hash

# Metabase (optional — leave blank to disable embedded dashboards)
# Maps to Metabase:EmbedSecret in appsettings.json
METABASE_EMBED_SECRET=
